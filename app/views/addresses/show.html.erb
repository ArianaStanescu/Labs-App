<section>
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-lg-5 mx-auto" style="margin-left: 350px;">
        <% if @address.address_type == 'both' %>
          <%= render 'users/address', address_type: "Shipping", address: @address, edit_link: edit_address_path(@address) %>
          <%= render 'users/address', address_type: "Billing", address: @address, edit_link: edit_address_path(@address) %>
        <% end %>
        <% if @address.address_type == 'shipping' %>
          <%= render 'users/address', address_type: "Shipping", address: @address, edit_link: edit_address_path(@address) %>
        <% end %>
        <% if @address.address_type == 'billing' %>
          <%= render 'users/address', address_type: "Billing", address: @address, edit_link: edit_address_path(@address) %>
        <% end %>
      </div>
    </div>
  </div>
  <div style="text-align: center;">
    <%= link_to "Back to addresses", addresses_path, class: "btn btn-primary" %>
  </div>
</section>
<br>
